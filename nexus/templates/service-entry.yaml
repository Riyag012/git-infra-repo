apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: nexus-external-entry
  namespace: nexus
spec:
  hosts:
  - nexus-sonatype-nexus-repository-manager.nexus.svc.cluster.local
  location: MESH_INTERNAL
  ports:
  - number: 8081
    name: http
    protocol: HTTP
  resolution: DNS
