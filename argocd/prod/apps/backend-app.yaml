apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alumni-backend-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/kanishkpandey18/git-infra-repo.git'
    targetRevision: main
    path: charts/alumni-backend
    helm:
      valueFiles:
        - ../../argocd/prod/values/backend-prod.yaml
  destination:
    # This targets the Prod Spoke cluster you registered in ArgoCD CLI
    name: prod-cluster
    namespace: alumni-portal-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true